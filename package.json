{
  "name": "proxy-backend",
  "version": "1.0.2",
  "description": "Proxy-backend is an HTTP programmable proxying library that supports websockets. It is suitable for implementing components such as reverse proxies and load balancers",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "scripts": {
    "test": "mocha --recursive --reporter spec",
    "watch-test": "mocha --recursive --watch --reporter spec",
    "test-ci": "istanbul cover ./node_modules/mocha/bin/_mocha test/**/*.js --report lcovonly -- -R spec && cat ./coverage/lcov.info"
  },
  "devDependencies": {
    "mocha": "^6.2.0",
    "chai": "^4.1.2",
    "chai-spies": "^1.0.0",
    "istanbul": "0.4.5",
    "unit-test-class": "1.0.7"
  },
  "keywords": [
    "http proxy",
    "proxy",
    "backend",
    "healthcheck",
    "monitor",
    "status"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/hisco/proxy-backend.git"
  },
  "bugs": {
    "url": "https://github.com/hisco/proxy-backend/issues"
  },
  "homepage": "https://github.com/hisco",
  "author": "Eyald <hisco@googlegroups.com‬>",
  "license": "MIT",
  "dependencies": {
    "http-proxy": "^1.16.2",
    "status-monitor": "^1.0.3"
  }
}
